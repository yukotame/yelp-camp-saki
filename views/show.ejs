<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= product.name  %></title>
</head>
<body>
    <h1><%= product.name %></h1>
    <ul>
        <li> 価格：<%= product.price %>円 </li>
        <li> カテゴリー：<a href="/products?category=<%= product.category %>"><%= product.category %></a> </li>
    </ul>
    <a href="/products">一覧へ</a>
    <a href="/products/<%= product.id %>/edit">編集へ</a>
    <form action="/products/<%= product._id %>?_method=DELETE" method="POST">
        <button>削除</button>
    </form>
</body>
</html>